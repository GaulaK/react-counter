import logo from "../assets/img/calculator-vector.png";

const Header = () => {
  return (
    <header>
      <div>
        <img alt="Logo Calculator" src={logo} />
      </div>
      <h1>React Counter</h1>
    </header>
  );
};

export default Header;
